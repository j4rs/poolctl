node_modules
dist
.DS_Store
*.local
.env
.env.*

# Local agent/editor scratch. launch.json pins a machine-specific Node
# toolchain, so it should not be prescribed to contributors.
.claude/
supervisor/node_modules
